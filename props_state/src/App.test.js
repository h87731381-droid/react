import { BrowserRouter as Router, Routes, Rpitesw  Route, NavLink } from 'react-router-dom'

import { render, screen } from '@testing-library/react';
import App from './App';
import ProductList from '../../self_1/src/pages/ProductList';
import ProductView from '../../self_1/src/pages/ProductView';

test('renders learn react link', () => {
  render(<App />);
    return (
      
    )

});
